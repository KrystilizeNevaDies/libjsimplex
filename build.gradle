plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
}

repositories {
    // Use jcenter for resolving dependencies.
    jcenter()
}

dependencies {
    // Aparapi
    compile "com.aparapi:aparapi:1.9.0"
    compile "com.aparapi:aparapi-jni:1.4.1"
}
